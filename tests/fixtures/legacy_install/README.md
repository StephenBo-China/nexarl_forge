# Legacy Install Fixture

This directory marks the legacy-install test fixture family. The actual fixture
data is assembled in the E2E test helpers so the tests can keep the on-disk
tree small and deterministic.
