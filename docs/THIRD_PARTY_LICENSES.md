# Third-party licenses

Nexarl Forge v1.0.0 has no third-party Python package dependencies at runtime. It uses the Python standard library and macOS system services (LaunchAgent/launchctl).

Development and verification tools are invoked from the contributor's environment and are not bundled or redistributed by this repository. If a future release bundles a dependency, its exact version and license will be added here before release.
